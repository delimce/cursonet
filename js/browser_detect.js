 if( (navigator.appName!="Microsoft Internet Explorer") && (navigator.appName!="Netscape")){
	 
			 if (confirm('Aparentemente su Navegador '+navigator.appName+' no es compatible con esta version de Cursonet, ¿desea intentar usar Cursonet de todas maneras?')) {
				 void("");
			 } else { 
				top.location.replace('http://www.firefox.com');
			 };
			 
			 
	 
 }